# Zoomi - Landing Page

Estrutura separada do projeto:

```
zoomi-separado/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── script.js
└── assets/
```

Para rodar localmente, abra o `index.html` no navegador ou use a extensão Live Server no VS Code.
